import {analyzeArray} from './analyzeArray'


describe('analyzeArray', () => {
    test('returns average, min, max and length of the array'), () =>{
        expect(analyzeArray([1,2,3])).toBe()
    }
})
